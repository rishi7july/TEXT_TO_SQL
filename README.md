The script has two parts 

1. sql.py 

2. app.py 

Whereas the sql.py creates a db called stundents and inserts entries . 

The app.py runs a streamlit app . The prompt is converted to SQL Query and Results are displayed . 

Important - Run the sql.py first and then the app.py 